import { StyleSheet } from "react-native";
import ColorDefault from '../../styles/ColorDefault';

const styles = StyleSheet.create({
    search: {
        borderRadius: 30,
        marginHorizontal: 20,
        paddingHorizontal: 10,
        alignItems: 'center',
        backgroundColor: ColorDefault.light,
        flexDirection: 'row'
    }
})
export default styles;