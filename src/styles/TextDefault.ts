import { StyleSheet } from "react-native";

const TextDefault = StyleSheet.create({
    text: {
        fontFamily: "Roboto",
        fontStyle: "normal",
        fontWeight: "bold",
    }
});

export default TextDefault;